'use strict'

const fp = require('fastify-plugin')

module.exports = fp(async function (fastify, opts) {

/*
    fastify.register(require('../lib/plugins/workflow'), {
    }, err => {
        if (err) throw err
    })
*/

})
